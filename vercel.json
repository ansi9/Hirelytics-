{
  "buildCommand": "cd artifacts/resume-shortlister && npm run build",
  "outputDirectory": "artifacts/resume-shortlister/dist",
  "installCommand": "pnpm install --frozen-lockfile",
  "routes": [
    {
      "src": "^/(?!.*\\.).*$",
      "destination": "/index.html"
    }
  ]
}
